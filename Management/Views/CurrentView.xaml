<UserControl x:Class="FacePass.Management.Views.CurrentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#0D0D0D"
             Unloaded="UserControl_Unloaded">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Header & Refresh Status -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="Active Lecture" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Monitor ongoing biometric &amp; QR attendance check-ins in real-time" FontSize="14" Foreground="#88FFFFFF" Margin="0,5,0,0"/>
            </StackPanel>
            
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal">
                <Ellipse Width="8" Height="8" Fill="#00E676" Margin="0,0,8,0" VerticalAlignment="Center">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Style.Triggers>
                                <EventTrigger RoutedEvent="Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="1" To="0.3" Duration="0:0:1" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <TextBlock x:Name="LastUpdatedText" Text="Syncing..." FontSize="13" Foreground="#00E676" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- Active Lecture / No Active Lecture Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            
            <!-- Card A: Ongoing Lecture Details -->
            <Border x:Name="ActiveCard" Background="#1A1A1A" CornerRadius="10" BorderBrush="#333" BorderThickness="1" Padding="20" Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <Border Background="#2E7D32" CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" Margin="0,0,0,10">
                            <TextBlock Text="● IN PROGRESS" FontSize="11" FontWeight="Bold" Foreground="#00E676"/>
                        </Border>
                        <TextBlock x:Name="CourseNameText" Text="Course Name" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock Text="🕒 " FontSize="14" Foreground="#00E676"/>
                            <TextBlock x:Name="TimeSlotText" Text="08:30 AM - 10:00 AM" FontSize="14" Foreground="#CCCCCC" Margin="5,0,20,0"/>
                            
                            <TextBlock Text="📍 " FontSize="14" Foreground="#00E676"/>
                            <TextBlock x:Name="RoomText" Text="CS Lab 2" FontSize="14" Foreground="#CCCCCC" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Border Grid.Column="1" Background="#111" CornerRadius="8" Padding="20,10" BorderBrush="#222" BorderThickness="1" VerticalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="PresentCountText" Text="0" FontSize="36" FontWeight="Bold" Foreground="#00E676" HorizontalAlignment="Center"/>
                            <TextBlock Text="Present So Far" FontSize="12" Foreground="#88FFFFFF" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Card B: No Lecture Ongoing -->
            <Border x:Name="NoActiveCard" Background="#1A1A1A" CornerRadius="10" BorderBrush="#333" BorderThickness="1" Padding="30" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="💤" FontSize="42" Foreground="#88FFFFFF" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="No Ongoing Lecture Found" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                    <TextBlock Text="There are no timetable slots matching the current day and time on your schedule." FontSize="13" Foreground="#88FFFFFF" HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Attendance Feed Grid -->
        <Border Grid.Row="2" Background="#1A1A1A" CornerRadius="8" BorderBrush="#333" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Attendance List (Present Today)" FontSize="16" FontWeight="Bold" Foreground="White" Margin="5,0,0,15"/>

                <DataGrid x:Name="PresentStudentsGrid" Grid.Row="1" Background="Transparent" Foreground="White" 
                          BorderThickness="0" RowBackground="#1A1A1A" AlternatingRowBackground="#151515"
                          AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="None"
                          CanUserAddRows="False" CanUserResizeRows="False" SelectionMode="Single"
                          HeadersVisibility="Column" HorizontalGridLinesBrush="#222">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Student Name" Binding="{Binding student_name}" Width="*"/>
                        <DataGridTextColumn Header="Time Checked In" Binding="{Binding timestamp}" Width="200"/>
                        
                        <DataGridTemplateColumn Header="Method" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,3" HorizontalAlignment="Left" Background="#222">
                                        <TextBlock Text="{Binding method_formatted}" FontSize="12" FontWeight="SemiBold" Foreground="#88FFFFFF"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Status" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding status_formatted}" FontWeight="Bold" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding status}" Value="present">
                                                        <Setter Property="Foreground" Value="#00E676"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding status}" Value="suspicious">
                                                        <Setter Property="Foreground" Value="#FF5252"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding status}" Value="manual_override">
                                                        <Setter Property="Foreground" Value="#00B0FF"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.Resources>
                        <!-- Header Styling -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#00E676"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="BorderBrush" Value="#333"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                        <!-- Row Styling -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="BorderBrush" Value="#222"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="Padding" Value="5"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#2A2A2A"/>
                                    <Setter Property="Foreground" Value="#00E676"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                        <!-- Cell Styling -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
    <Setter.Value>
        <ControlTemplate TargetType="DataGridCell">
            <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                <ContentPresenter VerticalAlignment="Center"/>
            </Border>
        </ControlTemplate>
    </Setter.Value>
</Setter>
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
