<Window x:Class="FacePass.Management.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FacePass Management" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen" Background="#0D0D0D">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#111" BorderBrush="#333" BorderThickness="0,0,0,1">
            <Grid Margin="20,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="FACE" FontWeight="Bold" Foreground="#00E676" FontSize="20"/>
                    <TextBlock Text="PASS" FontWeight="Bold" Foreground="White" FontSize="20"/>
                    <TextBlock x:Name="RoleLabel" Text="| TEACHER PORTAL" Foreground="#88FFFFFF" Margin="10,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock x:Name="UserNameLabel" Text="Welcome, User" Foreground="White" Margin="0,0,20,0" VerticalAlignment="Center"/>
                    <Button Content="Logout" Background="Transparent" Foreground="#FF5252" BorderThickness="0" Click="Logout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <ContentControl Grid.Row="1" x:Name="MainContent"/>

    </Grid>
</Window>
