<Window x:Class="FacePass.Management.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FacePass Management Login" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" Background="#0D0D0D"
        WindowStyle="None" AllowsTransparency="True">
    
    <Border BorderBrush="#333" BorderThickness="1" CornerRadius="15" Background="#111">
        <StackPanel Margin="40" VerticalAlignment="Center">
            
            <TextBlock Text="FacePass" FontSize="32" FontWeight="Bold" Foreground="#00E676" 
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Management Portal" FontSize="14" Foreground="#88FFFFFF" 
                       HorizontalAlignment="Center" Margin="0,0,0,40"/>

            <TextBlock Text="Email Address" Foreground="#88FFFFFF" Margin="0,0,0,5"/>
            <TextBox x:Name="EmailBox" Height="40" Background="#1A1A1A" Foreground="White" 
                     BorderBrush="#333" VerticalContentAlignment="Center" Padding="10,0"/>

            <TextBlock Text="Password" Foreground="#88FFFFFF" Margin="0,20,0,5"/>
            <PasswordBox x:Name="PassBox" Height="40" Background="#1A1A1A" Foreground="White" 
                         BorderBrush="#333" VerticalContentAlignment="Center" Padding="10,0"/>

            <Button x:Name="LoginBtn" Content="LOGIN" Height="45" Margin="0,40,0,0" 
                    Background="#00E676" Foreground="Black" FontWeight="Bold" 
                    BorderThickness="0" Click="LoginBtn_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="CloseBtn" Content="Exit Application" Background="Transparent" 
                    Foreground="#55FFFFFF" Margin="0,20,0,0" BorderThickness="0" 
                    Click="CloseBtn_Click"/>

        </StackPanel>
    </Border>
</Window>
