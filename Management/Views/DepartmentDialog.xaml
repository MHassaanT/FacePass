<Window x:Class="FacePass.Management.Views.DepartmentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Department" Height="230" Width="380"
        WindowStartupLocation="CenterOwner" Background="#111"
        WindowStyle="ToolWindow" Foreground="White">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="Department" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <TextBlock Text="Department Name" Foreground="#88FFFFFF" Margin="0,0,0,5"/>
            <TextBox x:Name="DepartmentNameBox" Height="35"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Save" Width="90" Height="40" Background="#00E676" Foreground="Black" FontWeight="Bold" Click="Save_Click"/>
                <Button Content="Cancel" Width="80" Height="40" Margin="10,0,0,0" Background="#333" Foreground="White" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
