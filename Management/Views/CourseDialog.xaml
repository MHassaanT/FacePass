<Window x:Class="FacePass.Management.Views.CourseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Course" Height="400" Width="430"
        WindowStartupLocation="CenterOwner" Background="#111"
        WindowStyle="ToolWindow" Foreground="White">
    <Window.Resources>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <StackPanel>
            <TextBlock Text="Course" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

            <TextBlock Text="Course Name" Foreground="#88FFFFFF" Margin="0,0,0,5"/>
            <TextBox x:Name="CourseNameBox" Height="35"/>

            <TextBlock Text="Course Code" Foreground="#88FFFFFF" Margin="0,15,0,5"/>
            <TextBox x:Name="CourseCodeBox" Height="35"/>

            <TextBlock Text="Department" Foreground="#88FFFFFF" Margin="0,15,0,5"/>
            <ComboBox x:Name="DepartmentCombo" Height="35" DisplayMemberPath="name"/>

            <TextBlock Text="Assigned Teacher" Foreground="#88FFFFFF" Margin="0,15,0,5"/>
            <ComboBox x:Name="TeacherCombo" Height="35" DisplayMemberPath="name"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
                <Button Content="Save" Width="90" Height="40" Background="#00E676" Foreground="Black" FontWeight="Bold" Click="Save_Click"/>
                <Button Content="Cancel" Width="80" Height="40" Margin="10,0,0,0" Background="#333" Foreground="White" Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
